import React from "react";

const AddTaskForm = () => (
  <header className="AddTaskForm ">
    <div>
      <form>
        <input t ype="text" placeholder="what needs to done?"></input>
        <button type="submit">add</button>
      </form>
    </div>
  </header>
);

export default AddTaskForm;
